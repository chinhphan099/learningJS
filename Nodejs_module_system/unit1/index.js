/**
 * 1. Require module math
 * 2. Sử dụng method `add` để tính tổng 2 số bất kì
 * 3. Log ra kết quả phép tính tổng ở trên
 */
var math = require('./math');
var tinhTong = math.add(11, 23);
console.log(tinhTong);
